{{ if eq .chezmoi.os "darwin" -}}
#!/bin/bash

# exit fast if already in $PATH
type gum >/dev/null 2>&1 && exit
export PATH="$PATH:/opt/homebrew/bin"

echo "Installing gum..."
brew bundle --file=/dev/stdin <<EOF
brew "gum"
EOF

{{ end -}}
