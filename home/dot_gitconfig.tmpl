[user]
    name = {{ .git_username }}
    email = {{ .git_email }}
    signingkey = {{ .git_signing_key }}

[init]
    defaultBranch = main

[core]
    editor = code --wait

[commit]
    gpgSign = true

# [url "git@github.com:"]
#     insteadOf = https://github.com/
